type: feature

`appsettings` is limited to bootstrap keys (database, key ring path, URLs, log level, bootstrap admin); every other key in an appsettings JSON file is ignored and named in one startup warning.
