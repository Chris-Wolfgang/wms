type: feature

Adopt the modular-monolith architecture (ADR 0001): a Core project with the module descriptor and collection, explicit `AddWmsModule`/`MapWmsModules` registration, and an architecture test that modules reference only Core and Domain.
