type: feature

Separate migration assemblies per database provider (`Wolfgang.Wms.Infrastructure.Migrations.SqlServer` / `.PostgreSql`) with the initial migration, generated and checked together by `scripts/Check-Migrations.ps1`.
