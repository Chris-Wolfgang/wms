type: feature

Add row-version HTTP validation (`EntityTag`, `CacheControl`, `ConditionalResults.NotModifiedOr`) and the per-instance `VersionedCache<T>` over `IRowVersionSource`, with ADR 0003 on read models and caching.
