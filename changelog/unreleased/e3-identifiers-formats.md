type: feature

Customer-supplied identifiers: validation profiles (case, trim, length, required, format) applied identically everywhere, mask formats (`AAA-999`, `9(8)`, `T-X(6)`) compiled to anchored regular expressions, regex evaluation that cannot stall a scan, and structural GS1 validation (application identifiers, check digits, dates) in the shared Domain.
