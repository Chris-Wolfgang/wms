type: feature

Setting storage: `core.setting` (scope_type, scope_id, key, configured_value, effective_value, row_version, updated_by, updated_at, deleted_at; unique per scope and key) with its row-version trigger, migrated on SQL Server and PostgreSQL.
