type: feature

Console workspaces: one Blazor Server host with Configure, Supervise, Resolve, Report and Insights in their own projects, a shared workspace layout with a license/permission entry gate (free tier until E79/E11), a workspace chooser on `/`, and a keyboard-wedge `ScanListener` component.
