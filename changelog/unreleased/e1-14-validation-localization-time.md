type: feature

Wire source-generated request validation, camelCase JSON and resource-file localization (`AddWmsLocalization`, `UseWmsRequestLocalization`, English only) into the API host; product assemblies expose `DateTimeOffset`, never `DateTime`.
