type: docs

Generate the settings, permissions, error-code and module references from the product's registries (`ReferencePages`, kept current by a test), add the troubleshooting page every error `type` links into, record the DocFX decision (ADR 0006), and serve the packaged docs site from the console at `/help/` (`ConsoleHelp`, `HelpLinks`).
