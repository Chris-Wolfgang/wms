type: feature

Add licensing (E79): the compiled-in free tier and this release's monotonic tier table (`TierTables.Current`, checked in CI against `docs/licensing/tier-table.txt`), signed JSON keys verified offline against the vendor's public key and stored encrypted as `license.keys`, stackable base and add-on keys composed into `ILicense`, the soft-limit gate with allowance and grace (`LicenseGate`, `license.limit_reached`), `RequireLicenseFeature` for feature edges, the license page (`GET/PUT/DELETE /system/license[/keys]`, `GET /system/license/comparison`) and the generated `docs/licensing/feature-comparison.md`.
