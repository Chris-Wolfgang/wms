type: feature

Sequence-backed `row_version` on versioned tables (column default on insert, provider update trigger, concurrency token, indexed) with the migration helper, and optimistic concurrency on the API: `If-Match` required on updates (428), a stale tag or stale save answers 412.
